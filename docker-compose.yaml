services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flask_app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - APP_NAME={APP_NAME}
      - API_KEY={API_KEY}
      - EXCHANGE_API_URL={EXCHANGE_API_URL}